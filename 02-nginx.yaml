- name: install and run nginx
  hosts: server
  become: yes # it will be get root access
  tasks: 
  - name: install nginx package
    ansible.builtin.package:
     name: nginx
     state: present
  - name: run nginx
    ansible.builtin.service:
     name: nginx
     state: started
